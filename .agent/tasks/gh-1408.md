# GH-1408

**Created:** 2026-02-17

## Problem

GitHub Issue #1408: Migrate docs site from Nextra 2 to Nextra 4 (App Router)

Parent: GH-1407

Complete end-to-end migration of the `docs/` site: update `package.json` to Next.js 15 + Nextra 4 + React 19 + pagefind; rename `pages/` → `content/`; convert all 9 `_meta.json` files to `_meta.js` (export default pattern); create `app/layout.tsx` (replacing `theme.config.tsx` — banner, navbar, footer, metadata, sidebar/toc config); create `app/[[...mdxPath]]/page.tsx` catch-all route; create `mdx-components.tsx` at docs root; update `next.config.mjs` (remove theme/themeConfig options); update `tsconfig.json` (moduleResolution → bundler, add next plugin, add .next/types to include); fix `content/features/replay.mdx` (`Tab` → `Tabs.Tab`); delete obsolete `theme.config.tsx` and `next-env.d.ts`; add `public/_pagefind/` to `.gitignore` and `git rm -r --cached` if tracked; update `docker/Dockerfile` to run `postbuild` (pagefind) after build. Verify `bun run build` succeeds with zero errors and all URLs are preserved.

## Acceptance Criteria

