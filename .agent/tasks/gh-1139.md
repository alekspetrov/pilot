# GH-1139

**Created:** 2026-02-14

## Problem

Largest extraction. Move all 14 progress-related functions: callback setters (`OnProgress`, `AddProgressCallback`, etc.), `reportProgress()`, stream event parsing (`parseStreamEvent`, `processBackendEvent`), and Navigator pattern handlers (`parseNavigatorPatterns`, `parseNavigatorStatusBlock`, `handleStructuredSignals`, `handleNavigatorPhase`, `handleToolUse`). Verify with `go build`.

## Acceptance Criteria

