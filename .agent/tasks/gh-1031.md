# GH-1031

**Created:** 2026-02-13

## Problem

Tests for `BuildPrompt` with populated knowledge/profile, post-task wiring with temp `.agent` dir and in-memory SQLite, and drift detector retry integration. Run `go build ./...` and `go test ./internal/executor/... ./internal/memory/...` clean.

## Acceptance Criteria

