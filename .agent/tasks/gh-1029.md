# GH-1029

**Created:** 2026-02-13

## Problem

After webhook dispatch (~line 2081): archive task doc via `ArchiveTaskDoc()`, create `ContextMarker` via `CreateMarker()`, and store a `memory.Memory` via `knowledge.AddMemory()` on success. All failures logged as warnings, never blocking.

## Acceptance Criteria

