# GH-1138

**Created:** 2026-02-14

## Problem

Move `runSelfReview()`, `buildQualityGatesResult()`, and the `simpleQualityChecker` struct + `Check()` method. These handle self-review execution and quality gate result formatting. Verify with `go build`.

## Acceptance Criteria

