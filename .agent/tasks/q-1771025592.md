# GH-1059: feat(config): add Config.Validate() with bounds and enum checks

## Status: Planning Complete

## Subtasks

### 1. Add bounds and orchestrator validation
Validate numeric ranges in `Config.Validate()` (`internal/config/config.go`):
- `orchestrator.max_concurrent` >= 1
- `quality.on_failure.max_retries` in range 0–10
- `budget.daily_limit` > 0 when budget enabled
- `orchestrator.execution.mode` must be "sequential" or "parallel"

Add table-driven tests in `internal/config/validation_test.go`.

### 2. Add alert channel type and quality gate type enum validation
- `alerts.channels[].type` must be one of: slack, telegram, email, webhook, pagerduty
- `quality.gates[].type` must be one of: build, test, lint, coverage, security, typecheck, custom
- Add to `Validate()` with tests for unknown and valid enum values.

### 3. Add adapter required-field validation
When adapter `Enabled == true`, validate required fields:
- GitHub: `token` non-empty
- Slack: `bot_token` non-empty
- Telegram: `bot_token` + `chat_id` non-empty
- Linear: `api_key` + `team_id` (or non-empty `workspaces`)

Token format warnings (log only, not error):
- Slack `bot_token` should start with `xoxb-`
- GitHub `token` should be non-empty when enabled

### 4. Add polling interval bounds checks
- `adapters.github.polling.interval` >= 10s (when polling enabled)
- `orchestrator.execution.poll_interval` >= 10s
- Tests for boundary values (10s, below 10s, zero)

### 5. Wire validation + integration test
- Validate() already called in Load() — confirm coverage
- Write `TestValidate_FullConfig` integration test: DefaultConfig passes, then break each field
- Run `go test ./internal/config/...`
